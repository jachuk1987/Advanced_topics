
import { AppBar, Toolbar} from "@mui/material";

const Header: React.FC = () => {
    return (
        <AppBar>
            <Toolbar>
                
            </Toolbar>
        </AppBar>
    )
}

export default Header;